<%= form_with(model: category, local: true) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this food from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% else %>
    <h2> Hello </h2>
  <% end %>

  <div class="field">
    <%= form.label :category %>
    <%= form.collection_select(:category_id, @categories, :name, id: :category_name, :include_blank => 'Please select') %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
